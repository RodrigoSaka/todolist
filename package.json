{
  "name": "todolist",
  "version": "1.0.0",
  "scripts": {
    "start": "npm-run-all -p -r start:html api",
    "start:html": "live-server src --port=3000",
    "api": "json-server ./api/db.json --delay 2000 --port 3001"
  },
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.17.4",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}